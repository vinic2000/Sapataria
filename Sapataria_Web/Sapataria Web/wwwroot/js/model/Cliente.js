class Cliente {
    constructor(titulo, cliente, d_entrada, descricao) {

        this.titulo = titulo,
            this.cliente = cliente,
            this.d_entrada = d_entrada,
            this.descricao = descricao
    }
}